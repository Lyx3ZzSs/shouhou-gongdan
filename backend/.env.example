# 开发环境配置模板
# 复制此文件为 .env 并根据需要修改
# cp .env.example .env

# 数据库连接（docker compose up -d 启动 PostgreSQL）
DATABASE_URL=postgresql+asyncpg://postgres:JvUcMbDxjYY4M8sj@localhost:5432/shouhou_gongdan

# Redis 连接（docker compose up -d 启动 Redis）
REDIS_URL=redis://localhost:6379/0

# 销售易 CRM 对接（可选，未配置时跳过同步）
# XIAOSHOUYI_BASE_URL=
# XIAOSHOUYI_API_KEY=
