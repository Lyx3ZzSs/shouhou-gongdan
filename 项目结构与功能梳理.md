# AI 售后智能服务一体化平台 — 项目结构与功能梳理

> 基于 `AI售后智能服务一体化平台需求资料/` 下全部资料整理。编制单位：国能日新科技股份有限公司。需求文档版本 V0.2（2026-05-27）。

---

## 一、项目概述

**定位**：面向新能源（功率预测、控制、储能）售后服务的「AI 驱动 + 人机协同 + 全渠道统一」智能服务平台。整合 AI 能力与现有 CRM，提升售后效率与客户满意度。

**核心痛点**：
- 售后渠道分散（400 电话、企业微信、邮件、小程序），数据割裂
- 业务复杂度高（算法 / 气象 / 考核规则），跨部门协同频繁
- 业务增长带来人力成本攀升，服务效率跟不上

**建设目标**：全渠道统一接入 → AI 自动受理与分流 → 智能工单生成与派单 → 跨部门协同闭环 → 数据驱动持续优化。

**设计原则**：人机协同优先（AI 处理标准化、人处理复杂）、全渠道统一接入、数据驱动决策、微服务可扩展、安全合规可审计。

---

## 二、整体架构

分层 + 微服务 + AI 能力中台：

| 层级 | 职责 | 核心组件 |
|---|---|---|
| 客户接入层 | 多渠道统一入口 | 400 电话、企业微信、邮件、微信小程序 |
| AI 智能接入与理解层 | 语音/文本理解、身份核验 | ASR、TTS、NLP、情绪识别、身份核验 |
| AI 智能处理与决策层 | 知识问答、工单决策 | RAG 问答、工单生成、问题分类、智能路由 |
| 业务服务层（微服务） | 售后业务闭环 | 受理单、派单、协同、回访、质检 |
| 数据与 AI 中台层 | 存储与向量检索 | MySQL、Redis、MongoDB、PostgreSQL 向量库、MinIO |
| 运维监控与安全体系 | 可观测与合规 | 日志、监控、告警、审计、RBAC 权限 |

**部署**：微服务 + API 网关、Kubernetes 容器化、CI/CD、多环境隔离。目标 RPO≤1h、RTO≤4h、可用性 99.9%。

---

## 三、核心功能模块

| 模块 (FR) | 职责 | 关键能力 |
|---|---|---|
| **FR-001 多渠道接入** | 统一接入 4 渠道，输出标准 JSON | 400 电话（CTI+ASR+IVR+情绪识别）、企微 API、邮件 IMAP/SMTP、小程序（一项目一二维码扫码绑定） |
| **FR-002 AI 身份核验** | 四重校验防串户 | 场站名称 + 项目编码 + 二维码绑定 + 历史号码；匹配度<90% 触发二次确认 |
| **FR-003 AI 智能处理 + 坐席辅助** | 分诊 + 辅助 + 工单生成 | RAG 问答（准确率≥90%、响应≤3s）、强制/智能转人工、1 秒弹屏、实时转写、话术推荐、自动生成工单 |
| **FR-004 智能派单与路由** | 多维规则自动派单 | 优先级：部门匹配 > 省份匹配 > 负载均衡 > 技能匹配；P1 自动升级；超时预警（响应 80% / 解决 50%） |
| **FR-005 跨部门协同** | 转派 + 上下文同步 + 闭环 | 部门内/跨部门转派（须填原因）、上下文自动同步、SLA 超时多级预警、复核+客户确认才算闭环 |
| **FR-006 质检与数据分析** | 看板 + 模型评估 + 绩效 | 实时看板、AI 准确率评估、bad case 回流、客户洞察、自动质检、坐席绩效 |

---

## 四、端到端业务流程

```
多渠道接入 → AI 身份核验(四重校验) → AI 受理分诊
   ├─ 简单问题：AI 直解并记录
   ├─ 中等问题：AI 辅助坐席
   └─ 复杂/触发条件：转人工(保留上下文)
→ AI 自动生成工单(补全信息+分类+优先级+责任部门建议)
→ 人工确认 → 工单池
→ 智能派单(部门>省份>负载>技能) → P1 自动升级
→ 跨部门协同(转派/子工单/上下文同步/超时预警)
→ 问题解决 → 复核 + 客户确认 → 闭环
→ 回访(D类 AI 外呼 / ABC类 人工) → 满意度采集
→ 数据分析 → 知识库/AI 模型/流程持续优化
```

### 400 电话接电导航细分（来自话术规范）
```
来电 → IVR(合力亿捷)
   ├─ 按1 功率预测问题 ─┐
   ├─ 按2 其他问题 ─────┴→ AI 智能坐席
   └─ 按3 投诉 → 转人工
AI 接入 → 获取场站名/来电号(缺失则追问) → 核实身份
  → 按1 精度问题 / 按2 设备故障报修
  → 记录+提示收资 → AI 生成受理单 → 结束语(有无其他问题→循环或挂机)
```

---

## 五、受理单数据模型（基于真实台账）

资料含两份大型台账（2025 全年 ~79,373 条 / 2026 上半年 ~47,033 条），**结构完全一致：1 个 Sheet、132 列**，是平台核心数据实体。按业务域分组：

| 业务域 | 代表字段 |
|---|---|
| 受理单基础 | 流水号、发起时间、受理单状态、受理单类型、受理单级别 |
| 项目/场站 | 场站名称、售后调度名称、项目编号(新/旧)、项目省份、场站等级、产品线/类别/类型、大客户简称 |
| 问题分类 | 问题分类、问题分类 1~5 级、问题类型、问题原因分类/备注 |
| 人员组织 | 发起人/部门、问题责任人/部门、一级部门、移交后责任人/部门、售后责任人、闭环回访人 |
| 客户与投诉 | 投诉发起时间、投诉人/联系方式、反馈渠道/次数、客户满意度、响应/处理时效满意度、服务态度评分 |
| 处理过程 | 问题详细描述、问题解决方案、方案反馈时间、要求解决时间、是否进场/延期/变更责任人 |
| 闭环跟踪 | 是否闭环、闭环回访时间/结果、问题实际解决时间、首次跟踪回访时间/结果 |
| 超时考核 | 方案反馈/解决是否超时、超时部门、服务截止日期、是否超期服务 |
| 挂起管理 | 挂起类别/原因、是否需要挂起、挂起开始/结束时间 1~4、挂起天数 1~4、挂起次数（支持最多 4 次挂起） |
| 系统/故障 | 流程状态_系统字段、故障等级、故障分类/明细、进场等级 |

**受理单状态生命周期**（实际数据分布验证）：
```
受理单创建 → 问题责任人处理 → 受理单跟踪 / 受理单挂起 / 第二次延期
  → 闭环回访 → 受理单闭环 → 受理单结束
```

**两年台账差异**：字段结构一致；2026 年受理单类型精简、新增「受理单挂起」状态、启用满意度细化评分采集、在途单据比例更高。

---

## 六、关键业务规则

### 受理单三级分类
- **产品问题**：配置 / 程序 / 新需求
- **数据问题**：精度 / 考核 / 分析报告
- **工程问题**：设备故障 / 安装调试 / 维护保养
- **采购问题**：设备采购 / 进场服务
- **其他问题**：咨询建议 / 投诉纠纷

### 工单主类型判定（关键词自动匹配，来自操作手册）
- **精度问题**：偏差大、考核多、不合格、优化、调整模型、不达标、短期/超短期、理论功率、限电、调和平均数…
- **现场问题**：故障、告警、死机、无法访问、界面打不开、二次安防、宕机、拉停、数据不展示、乱码、AGC 自限电…
- **混合关键词**：同时命中 → 拆分为两张独立 A 类受理单，分别派单并行处理

### 派单规则
- **精度工单**：按场站省份分配负责人（李燕昆 / 朱莉 分省）
- **现场工单**：按场站省份匹配对应区域工程师（北疆/东疆/南疆/青海西藏/蒙西/蒙东…分省到人）
- 通用优先级：部门匹配 > 省份匹配 > 负载均衡 > 技能匹配

### 转人工规则
- **强制转人工**：精度异常、考核争议、投诉、情绪不满、P1 紧急
- **智能转人工**：连续 3 次不满、AI 置信度<80%、对话超 5 轮未解决

### SLA 标准
| 级别 | 响应 | 解决 |
|---|---|---|
| P1 紧急 | 15 分钟 | 4 小时 |
| P2 高 | 1 小时 | 24 小时 |
| P3 中 | 4 小时 | 3 天 |
| P4 低 | 24 小时 | 7 天 |

### 异常处理
- 缺失场站省份 → 暂停生成，AI 追问补全
- 无匹配关键字 → 标记「待人工复核」转人工
- 省份无对应负责人 → 标记「异常工单」推送管理账号
- 预测补点工单 → 标注「当日完成」+ 加急
- A 类工单 → 优先派单

### 场景话术覆盖（来自场景表）
- **精度问题 7 情景**：带情绪直诉 / 描述现象 / 甩锅潜在投诉 / 会议需求 / 报告需求（调度通报） / 停电检修 / 历史数据导出
- **现场故障 4 情景**：系统告警 / 硬件故障 / 需进场 / AGC 相关

---

## 七、技术选型

| 层 | 选型 |
|---|---|
| 前端 | Web 坐席工作台（核心）、管理后台、移动端 H5 |
| 后端微服务 | 受理单 / AI 对话 / 工单 / 派单 / 协同 / 质检 / 知识库 服务 |
| AI 能力 | ASR、TTS、NLP（意图/分类）、RAG 检索增强、情绪识别、LLM 工单生成 |
| 关系库 | MySQL 8.0+（结构化业务数据） |
| NoSQL | Redis（缓存/会话）、MongoDB（日志/对话） |
| 向量库 | PostgreSQL 向量 / Pinecone / Milvus（知识库语义检索） |
| 对象存储 | MinIO / OSS / S3（语音、图片、视频、附件） |
| 基础设施 | Kubernetes、Prometheus、TLS+AES、RBAC |
| 评测技术栈 | pytest、httpx、Pydantic、JSON Schema、SciPy（匈牙利算法）、DeepDiff、pytest-html/JUnit、Git+JSONL、Excel |

**硬件参考**：应用服务器 8 核 16G、数据库主从 16 核 32G、AI 推理 GPU T4+ ≥2 台、存储 12 核 32G 2TB SSD。

---

## 八、非功能需求

| 维度 | 要求 |
|---|---|
| 性能 | UI 响应≤3s、语音转写<2s、知识推荐≤2s、受理单创建/派单≤10s、≥10 并发坐席、吞吐≥5 单/秒、ASR 准确率≥95%、意图识别≥90% |
| 可靠性 | MTBF>1000h、MTTR<30min、单节点故障不影响整体 |
| 可用性 | 7×24、年可用性≥99.9%、关键操作≤3 步 |
| 安全 | RBAC、TLS 1.2+ / AES-256、审计日志、防注入/XSS/DDoS、脱敏、敏感操作二次认证 |
| 可维护 | 模块化低耦合、日志保留 180 天、规则配置化热加载 |
| 可扩展 | 水平/垂直扩展、模型在线更新、RESTful API、插件扩展 |
| 兼容 | Chrome/Firefox/Edge/Safari 近两版、iOS 12+/Android 8+、与 CRM/ERP/OA 集成 |

**外部接口**：CRM（RESTful/JSON 双向同步）、ASR/TTS、NLP/知识库、邮件 IMAP/SMTP、企微 API、电话 SIP/CTI、Prometheus。通信协议 HTTP/HTTPS/WebSocket，数据格式 JSON/XML/CSV。

---

## 九、AI 工单生成准确性评测方案（项目特色）

采用「开源测试底座 + 自研业务评测规则」的轻量方案：只自研黄金数据规则和业务字段评测器，通用能力复用成熟开源组件。

### 评测指标与目标
| 指标 | 目标 |
|---|---|
| P0 严重错误 | 0 |
| JSON Schema 合规率 / 必填字段完整率 | 100% |
| 客户准确率 / 项目准确率 | ≥99% |
| 工单事实精确率 / 召回率 | ≥95% |
| 完整分类路径准确率 | ≥90% |
| 工单数量准确率 | ≥98% |
| 端到端 P95 响应 | ≤10s |

**P0 严重错误**：错关联客户/项目、跨客户串用信息、臆造不存在事实、应追问/转人工却直接派发、测试请求产生真实工单、敏感数据泄露。

### 黄金数据集
首版 400 条：校准集 250 + 固定回归集 100 + 盲测集 50；邮件与企微各≥150 条。覆盖单问题、信息缺失、多问题、多候选、重复/补充/无效、长文本噪声、附件/错误码、分类边界、紧急路由等场景。

建立流程：历史工单/真实消息 → 脱敏去重/线程合并 → A/B 双标 → 自动差异比较 → 业务专家仲裁 → Schema/泄漏/重复检查 → 冻结黄金版本。**历史工单仅作候选和预标注，不能直接作为黄金答案。**

### 评测方法
1. `X-Dry-Run: true` 调用工单生成 API（不创建真实工单，幂等）
2. 校验响应 Schema 与版本信息（服务/模型/Prompt/规则版本）
3. 比较是否应生成工单、比较工单数量
4. 多工单场景用 SciPy 匈牙利算法最佳配对
5. 逐字段业务评测：客户、项目、分类路径、事实、优先级、路由、完整性
6. 严重错误判断 + 多维报告（HTML/JUnit/JSON）

### 结果归因
PASS / FAIL_GENERATOR（生成服务）/ FAIL_UPSTREAM（渠道解析）/ FAIL_RULE_DATA（分类主数据规则）/ INVALID_GOLD（黄金答案需修正）/ REVIEW（等价/争议）/ ENV_ERROR（环境异常，不计准确率）。

### 演进预留
轻量版暂不建 Langfuse / Promptfoo / LLM-as-a-Judge / 全量生产评测，但架构预留接口；后续对比增多引入 Promptfoo，人工复核成瓶颈再引入 Langfuse。

---

## 十、项目计划与 WBS（2026-05-01 ~ 2026-09-01）

| 阶段 | 时间 | 关键交付 |
|---|---|---|
| 一、方案与架构 | 05-01 ~ 05-31 | 痛点调研、需求规格说明书 V1.0、流程图/原型、总体架构设计、方案冻结 |
| 二、服务工单接入系统开发 | 06-01 ~ 07-31 | 工单基础模块、小程序/邮件/400/企微四渠道、坐席辅助工作台、初版联调 |
| 三、智能工单与调度系统开发 | 07-01 ~ 08-05 | 智能工单生成引擎、去重/合并、智能派单、工单审核、预警通知、AI 外呼回访、全量测试 |
| 四、系统联调与内部测试 | 08-01 ~ 08-15 | 端到端联调、全链路压测、预警联调、全量功能测试与 Bug 闭环、试运行环境+培训 |
| 五、灰度上线 & 全量上线 | 08-15 ~ 09-01 | 灰度试运行(10%-30%)、迭代优化、全量上线筹备、09-01 正式全量上线 |

横向通用工作（全周期）：全流程测试保障、AI 算法持续优化、运维与环境保障、业务需求与运营保障、项目管理与进度管控。

**关键里程碑**：05-31 需求架构冻结 / 07-31 工单系统初版+渠道完成 / 08-05 智能模块完成 / 08-15 全量测试+试运行 / 08-15~25 灰度 / 09-01 全量上线。

---

## 十一、资料文件索引

| 文件 | 内容 | 用途 |
|---|---|---|
| `AI售后智能服务一体化平台总体设计.md` | 分层架构、模块、技术选型、业务流程、技术难点 | 架构骨架 |
| `AI售后智能服务一体化平台需求规格说明书_v0.2(1).docx` | 角色、FR-001~006、非功能、业务规则、数据接口 | 需求基线 |
| `AI售后自动工单生成准确性评测技术方案_轻量版.md` + `build_tech_solution_docx.py` | 评测指标、黄金数据、评测方法、归因 | 评测工程 |
| `1、AI智能坐席400接电导航标准话术&处理流程规范.docx` | IVR 导航、话术、分流 | 接电流程 |
| `2、AI智能坐席受理单生成标准操作手册.docx` | 工单类型判定、受理单生成 4 步、派单分省规则 | 受理单 SOP |
| `3、AI现场及精度场景及话术.xlsx` | 精度 7 情景 + 现场 4 情景话术 | 场景语料 |
| `4、质检标准.xlsx` | 电话/在线质检维度、客诉 SOP | 质检规则 |
| `AI售后系统项目工作拆解清单.xlsx` | 五阶段 WBS、里程碑、交付物、负责人 | 项目计划 |
| `总-受理单流程台账-2025年全年.xlsx`（60M，~79k 条） | 132 字段真实受理单数据 | 数据模型/样本 |
| `总-受理单流程台账-2026年1-6月受理单.xlsx`（39M，~47k 条） | 同结构，含挂起状态/满意度评分 | 数据模型/样本 |
| `呼叫系统通话录音*.zip` / `录音_*.zip` | 真实通话录音 | ASR/评测语料 |
